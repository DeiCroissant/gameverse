# Guide Thread Page Implementation

## Goal
Create `guide-thread.html` to display detailed guide threads similar to classic GameFAQs layouts but modernized.

## Tasks
- [x] Create `guide-thread.html` with breadcrumb and empty container.
- [x] Create `js/thread-loader.js` to parse URL parameter `?id=` and generate mock guide comments.
- [x] Add `.post-block`, `.post-header`, `.post-body`, and `.post-signature` styles to `css/style.css`.
