# Guide Pagination Implementation

## Goal
Implement JS pagination and dynamic linking for `guide.html`.

## Tasks
- [x] Create `js/guide-data.js` with mock data of 8 items.
- [x] Create `js/guide-loader.js` with pagination logic, slicing the array and rendering rows dynamically.
- [x] Add click events to redirect to `guide-thread.html?id=...`.
- [x] Link `js/guide-loader.js` to `guide.html` as a module.
- [x] Clear existing hardcoded rows from `<tbody>` in `guide.html`.
